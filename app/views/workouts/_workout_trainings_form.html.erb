<div class="nested-form-wrapper" data-new-record="<%= form.object.new_record? %>">
  <%= current_user.exercises.find_by(id: training.exercise_id).name %>
  <button type="button" data-action="nested-form#remove">Remove training</button>
  <%= form.hidden_field :_destroy %>
</div>
